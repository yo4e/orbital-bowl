/* ORBITAL BOWL / Orbital Reverie: React is only the picture frame around the full-screen game canvas. */
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
